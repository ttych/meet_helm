---
kind: Service
apiVersion: v1
metadata:
  name: clinic-service
spec:
  selector:
    app: clinic-front
  ports:
  # Default port used by the image
  - port: {{ .Values.service.port }}
    targetPort: 8080
